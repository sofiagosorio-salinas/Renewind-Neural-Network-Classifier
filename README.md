# Renewind-Neural-Network-Classifier
Neural network classifier predicting wind turbine generator failures from 40 sensor-derived predictors to enable predictive maintenance. Iteratively tunes architecture, batch size, weight initialization, momentum, and optimizer (SGD vs. Adam) across 6 model variants, optimizing recall to minimize costly missed failures.
